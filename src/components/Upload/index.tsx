import React from 'react';
import { Upload as Comp } from 'antd';
import DragAndDrop from './DragAndDrop';

const Upload = (props: any) => {
  return <Comp {...props} />;
};

const InputFile = (props: any) => {
  return <Comp {...props} />;
};

const Dragger = (props: any) => {
  return <Comp.Dragger {...props} />;
};

const UploadDragger = (props: any) => {
  return <Comp.Dragger {...props} />;
};

const FileDragger = (props: any) => {
  return <Comp.Dragger {...props} />;
};

export { Upload, InputFile, Dragger, UploadDragger, FileDragger, DragAndDrop };
