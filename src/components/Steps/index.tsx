import React from 'react';
import { Steps as Comp } from 'antd';

const Steps = (props: any) => {
  return <Comp {...props} />;
};

const Step = (props: any) => {
  return <Comp.Step {...props} />;
};

export { Steps, Step };
