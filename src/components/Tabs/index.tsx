import React from 'react';
import { Tabs as Comp } from 'antd';

const Tabs = (props: any) => {
  return <Comp {...props} />;
};

const TabPane = (props: any) => {
  return <Comp.TabPane {...props} />;
};

export { Tabs, TabPane };
