import React from 'react';
import { Mentions as Comp } from 'antd';

const Mentions = (props: any) => {
  return <Comp {...props} />;
};

const MentionsOption = (props: any) => {
  return <Comp.Option {...props} />;
};

export { Mentions, MentionsOption };
