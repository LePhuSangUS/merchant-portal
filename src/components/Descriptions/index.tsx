import React from 'react';
import { Descriptions as Comp } from 'antd';

const Descriptions = (props: any) => {
  return <Comp {...props} />;
};

const DescriptionItem = (props: any) => {
  return <Comp.Item {...props} />;
};

export { Descriptions, DescriptionItem };
