import React from 'react';
import { Dropdown as Comp } from 'antd';

const Dropdown = (props: any) => {
  return <Comp {...props} />;
};

const DropdownButton = (props: any) => {
  return <Comp.Button {...props} />;
};

export { Dropdown, DropdownButton };
